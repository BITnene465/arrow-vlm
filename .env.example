# Optional one-stage inference fallback only.
# If --checkpoint is not passed to scripts/infer.py or app/demo.py,
# they can fall back to CHECKPOINT_PATH from .env.
CHECKPOINT_PATH=outputs/your_experiment/checkpoints/best
